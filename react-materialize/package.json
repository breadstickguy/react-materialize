{
  "name": "react-material",
  "version": "0.3.0",
  "description": "Implementing React components using Materialize",
  "main": "lib/index.js",
  "dependencies": {
    "babel": "^5.2.17",
    "babel-core": "^5.2.17",
    "classnames": "^2.1.1",
    "materialize-css": "^0.96.1",
    "react": "^0.13.3"
  },
  "scripts": {
    "dev": "rm -rf ./lib && mkdir ./lib && babel -w -L -D src/ --out-dir lib/ --stage 1",
    "build": "rm -rf ./lib && mkdir ./lib && babel -L -D src/ --out-dir lib/ --stage 1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@bitbucket.org/jmstiefer/react-material.git"
  },
  "keywords": [
    "react",
    "material",
    "materialize"
  ],
  "author": "Joshua Stiefer",
  "license": "MIT"
}
